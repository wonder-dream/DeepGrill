# Python 编译产物：`scripts/` 下的脚本一跑（或调试时 import）就会生成，不该进库
__pycache__/
*.pyc

# 虚拟环境与包元数据（ADR-0010：独立 venv，与 v1 的 .venv 隔离）
.venv/
*.egg-info/

# uv 的缓存（沙箱不允许写用户级缓存目录，所以指向工作区内）
.uv-cache/

# pytest 的临时目录（basetemp）与实验用的临时目录。
# v1 的失败根因之一就是测试依赖沙箱不可写的系统临时目录 —— 见 pyproject.toml。
.tmp/
.runtmp/
.runtmpx/
.pytest-tmp/
.scratch/
pytest-cache-files-*/

# 运行期数据：SQLite 库是运行产物，不是源码。
# 题目的权威来源是 v1 的 data/interview.db（只读），v2 的库由
# `python -m migrations.run` 生成，因此不需要进库。
data/

# 密钥与机器相关的配置。**这一条是安全底线**：`app/config.py` 会读 `.env`，
# 而它里面装的是 LLM API key —— 一旦进库就是一次真实的凭据泄露
# （v1 就把真实 key 与 cookie 打进过部署包，见 docs/v1现状-20260913.md §6-P4）。
# 要提交模板就提交 `.env.example`，它下面有单独的例外。
.env
.env.*
!.env.example

# 编辑器
.idea/
.vscode/

# 审阅期的生成物：`tools/_compare_schema.py` 的输出。
# 它是**某一次审阅的快照**，schema 一变就过期 —— 所以不进库，
# 需要时重跑那条命令即可（工具的局限写在它自己的输出里）。
docs_compare.md

# 对照工具自检的临时目录（它自己会清理；这里是防中断后残留）
tools/_selftest_tmp_*/
