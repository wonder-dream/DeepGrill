## 2026-08-07 18:24

## 代码审查报告 — M1-M3 地基（跨模块）

### 审查目标
- 类型：本地改动（地基层，按计划 M1-M3 完成后统一审查）
- 范围：`app/errors.py`、`app/config.py`、`app/models.py`、`app/db.py`、`app/llm/llm_client.py`、`tests/fakes.py`、`tests/test_{config,models,llm_client}.py`、`config.yaml`、`requirements.txt`
- 审查时间：2026-08-07 18:24

### 审查输入证据
- 预检：`uv run pytest` → **46 passed** ✓
- 实证：FK 未强制（孤儿 Attempt session_id=99999 成功入库）
- 设计契约：MODULES.md §1.2/§2 M1-M3、docs/modules/M01-M03

### 问题清单

#### 🔴 严重问题
无

#### 🟡 建议修改
**1. SQLite 外键未强制（数据完整性）**
- 位置：`app/db.py` init_db
- 问题：SQLite 默认 `PRAGMA foreign_keys=OFF`（已实证孤儿行可入库），DB 层对 FK 无兜底
- 建议：init_db 注册 `@event.listens_for(engine, "connect")` 执行 `PRAGMA foreign_keys=ON`，并补回归测试（伪造 session_id 提交 → StorageError）；删 Session 走 ORM delete-orphan，不受影响

**2. API key 取值缺收敛点**
- 位置：`app/config.py` / `app/llm/llm_client.py`
- 问题：config 只存 env 变量名（M1 既定决策），M8/M12/M14 需要 key 值时会各自写 `os.environ[cfg.llm.api_key_env]`，三处重复
- 建议：出现第一个调用方（M8）时加 `config.secret_value(env_name)` 小 helper 收敛；现在不加（避免 speculative）

#### 🟢 可选优化
**3. `choices[0]` 空列表抛裸 IndexError**
- 位置：`app/llm/llm_client.py` complete()
- 建议：低概率，可加 `if not resp.choices: raise LLMError(...)`

**4. 测试跨文件 env 泄漏脆弱性**
- 位置：`tests/test_config.py`
- 建议：dotenv 直接写 os.environ，依赖 monkeypatch 恢复；pytest 默认按文件顺序运行目前稳定，可后续加 autouse 清理 fixture

**5. `AppConfig` 未复用 `_FrozenModel`**
- 位置：`app/config.py`
- 建议：M1 遗留，低优

**6. `Question.difficulty` 无范围约束**
- 位置：`app/models.py`
- 建议：SQLite 不检查范围，由 M8 生成时约束（1-5）

**7. requirements 未锁版本**
- 建议：已迁 uv，后续 `uv pip compile` 生成锁文件

### 亮点
- 模块间契约清晰：`retryable` 标记 + 异常链 `from e`，错误层次完整
- M2 三个 SQLModel 坑（scalars/CHECK 约束/delete-orphan）均被测试锁定，无静默陷阱
- 重试完全自控（`max_retries=0` 屏蔽 SDK 内置），退避序列可断言；4xx 不重试、5xx/超时/连接重试 2 次
- 全离网测试（MockTransport/tmp_path/monkeypatch），46 用例 1.9s
- `DuplicateSource` 携带 hash 值（从 IntegrityError 提取），幂等标记可用

### 结论
- 结论：`Approved`（无 🔴）
- 严重问题：0 个
- 建议修改：2 个
- 可选优化：5 个
- 下一步建议：🟡1（FK）本轮修复（约 6 行 + 1 回归测试）；🟡2 待 M8 出现调用方时处理；🟢 项延期

---

## 2026-08-07 22:10

## 代码审查报告 — M8/M9 核心生成链路

### 审查目标
- 类型：本地改动（M8 题目生成 + M9 去重）
- 范围：pp/pipeline/generate.py、pp/pipeline/dedup.py、	ests/test_generate.py、	ests/test_dedup.py
- 审查时间：2026-08-07 22:10

### 审查输入证据
- 预检：uv run pytest → **137 passed** ✓
- 实证：非 str stem（list）被 str() 转成 "['不是字符串']" 入库
- 设计契约：MODULES.md §2 M8/M9、docs/modules/M08/M09

### 问题清单

#### 🔴 严重问题
无

#### 🟡 建议修改
**1. 非 str 类型的 stem 产出垃圾题干**
- 位置：pp/pipeline/generate.py _validate_questions
- 问题：str(item.get("stem", "")) 把 list/数字等错误类型的 stem 强转成垃圾文本（已实证 ["不是字符串"] → "['不是字符串']"）并入库，污染题库
- 建议：stem 非 str 类型直接丢弃（与非法 type 同路径），3 行修复 + 回归测试

**2. 批内重复缺口（跨模块，M12 修复点）**
- 位置：pp/pipeline/dedup.py（语义）
- 问题：dedup 只对库内旧题判重；两条不同面经生成同题且库内无旧题命中时，同批两条都通过去重入库（当天重复，次日自愈）
- 建议：M12 将已接受的新题累积进 existing 池（batch-accumulate）再判后续条目；本模块签名不变

#### 🟢 可选优化
**3. generate.py 的 logger 未使用（死代码；M12 负责记 task_logs）**
**4. existing_stems 无单条截断（50 条超长题 → prompt 膨胀）；tags 无数量上限**
**5. M9 _llm_judge 只捕获 LLMError/LLMJsonError，意外异常会上抛（调用方只传 M3 客户端，可控）**
**6. M9 claimed 未命中不缓存 → 同批第二条对同一候选集重复 LLM 调用（语义正确，效率可接受）**

### 亮点
- M8/M9 均零落库、纯内存返回，与 M9/M12 衔接语义正确（去重与 D16 计数不被污染）
- 降级路径（宁重复勿丢题）三类失败全部覆盖且有测试锁定
- claimed 机制把文档"同批只保留第一条"落实为可断言的 LLM 调用优化（calls==1 测试）
- prompt 版本化常量命名一致（V1），格式错误由测试兜住
- 哈希粗筛纯函数（NFKC/去标点/lower/截断）干净，Phase 2 换 embedding 只动内部

### 结论
- 结论：Approved（无 🔴）
- 严重问题：0 个
- 建议修改：2 个
- 可选优化：4 个
- 下一步建议：🟡1 本轮修复（3 行 + 1 测试）；🟡2 记入 M12 实现清单

---

## 2026-08-07 23:45

## 代码审查报告 — 项目总审查（M1-M14 收尾）

### 审查目标
- 类型：本地改动（全模块收尾总审）
- 范围：app/ 全部 14 模块 + tests/ + README + 文档
- 审查时间：2026-08-07 23:45

### 审查输入证据
- 预检：uv run pytest → **198 passed** ✓
- 端到端冒烟：真实流水线 + 静态页 + openapi + 调度器注册全部通过；牛客源 301 被逐源隔离优雅降级（真实网络验证）
- 设计契约：docs/DESIGN.md / MODULES.md / modules/ 14 份

### 问题清单

#### 🔴 严重问题
无

#### 🟡 建议修改
**1. CLI 简历导入未接 M8 生成（已修复）**
- 位置：pp/__main__.py
- 问题：M5 职责明确"简历导入同时触发 project 题生成"，但 CLI --type resume 未传 project_generator，简历导入后不生成 project 题
- 修复：resume 类型时加载 config + 构建 LLMClient + 注入 generator（受 project_limit 节流）；manual 导入不加载 config（不强制 env），已冒烟验证

#### 🟢 遗留优化（历史审查累积，全部延期）
- M3 choices[0] 空列表 IndexError 兜底（低概率）
- requirements 未锁版本（可 uv pip compile）
- _FrozenModel 复用（M1 遗留）
- generate.py 未使用 logger（死代码）
- M9 _llm_judge 异常捕获范围 / claimed 未命中缓存
- 测试跨文件 env 泄漏脆弱性（pytest 顺序运行目前稳定）
- Question.difficulty 无 DB 级范围约束（M8 已钳制 1-3）

### 亮点
- 14 模块全部交付，跨模块契约一致：错误层次（retryable 标记）、依赖注入（llm/sources/runner 全部可替换）、幂等（哈希去重）、降级（宁重复勿丢题/安静停用/固定提示）
- 所有关键设计缺口均由测试暴露并修复：chain 终态识别、判分失败重试路径、expire_on_commit 陷阱、FastAPI 线程池内存库陷阱、CLI 生成链接、SQLite FK/CHECK 强制
- 198 用例全离线（MockTransport/tmp git 仓库/文件 SQLite），M7 无 git 自动跳过
- 文档-实现偏差全部回写（M01-M03、M13/M14 偏差记录），DESIGN/MODULES 状态同步为"实现完成"

### 结论
- 结论：Approved（无 🔴）
- 严重问题：0 个
- 建议修改：1 个（已修复）
- 可选优化：7 个（延期）
- 下一步建议：项目收尾完成；遗留 🟢 项按需迭代，建议优先 uv pip compile 锁版本与 README 配图

---
