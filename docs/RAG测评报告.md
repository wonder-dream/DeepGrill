# RAG 测评报告（卡码 Go 模块，2026-08-10）

> 目的：验证多查询 + 混合检索（Advanced RAG 第一阶段）的实际效果
> 数据：卡码笔记 Go 分类 40 题抓取导入（172 块）+ 题库跨主题负样本

## 一、检索层评估（scripts/eval_rag.py，离线零 LLM）

**正样本**：40 道 Go 题（query=问题标题，相关块=该页 slug）

| 配置 | Hit@5 | MRR@5 | Precision@5 |
|---|---|---|---|
| A 单查询纯向量 | **100%** | 0.946 | 0.625 |
| B 多查询纯向量 | 100% | 0.946 | 0.625 |
| C 多查询+文本路 | 97.5% | 0.896 | 0.600 |

**负样本**（Redis/MySQL/Java 题库题 20 道）：三配置命中率均 90%。

**深度诊断（10 题 top1 相似度核查）**：多数"命中"实为**语义真相关**（"线程状态"→goroutine/线程对比、"租约锁"→死锁、"GMP 调度"→GMP 块）；**真误命中仅 ~20%**（"Explain type"→Go 值类型=符号巧合、"Python vs Java"→Agent 文档=语义兜底 0.55-0.6）。

## 二、生成层评估（scripts/eval_rag_gen.py，LLM 裁判）

**样本**：8 道 Go 题 × 判分（注入/不注入）× 对比式裁判（A/B/tie，2 次取多数）

| 结果 | 占比 |
|---|---|
| 注入更优 | **50%**（4/8） |
| 相当 | 37.5%（3/8） |
| 不注入更优 | 12.5%（1/8） |

**关键发现**：
1. **注入有正收益且无明显伤害**（50% 胜出 vs 12.5% 落败，参考回答更完整——含调度时机等细节）
2. **方法论教训**：首版用绝对打分裁判（0-1），注入组内容明显更好却得 0.00 分（偏差）——**LLM 绝对打分不可靠，pairwise 对比才可信**（业界共识）

## 三、结论

| 维度 | 结论 |
|---|---|
| 检索召回 | ✅ 达标（Go 题 100% 命中自己的答案块） |
| 主题隔离 | ⚠️ 阈值 0.5 拦不住语义兜底（真误命中 ~20%，命中块多为语义相关） |
| 多查询/文本路 | 召回饱和下增益有限（A=B，C 略降）——保留但非关键 |
| 注入收益 | ✅ 生成层 pairwise 证实正收益（50% 更优） |

## 四、下一步

1. ~~第二阶段（相关性自评）~~ → **已完成，见下方增补**
2. **卡码扩展抓取**：/base/、/java/、/llm/ 分类（Go 已覆盖）
3. 抓取内容仅本地个人 RAG 使用（不对外分发、不商用）

---

## 增补：第二阶段（相关性自评 + 压缩精炼）测评结果（2026-08-10）

**实现**：`_knowledge_for` 检索 top-8 候选 → LLM 一次调用自评相关（kept）+ 提炼要点（condensed ≤1200 字带出处）→ 全不相关返回 None；失败降级原文拼接。

**真实场景验证**：
- 误命中拦截 ✓："Explain type"→None（符号巧合）、"Python vs Java"→None（语义兜底）
- 语义相关保留 ✓："线程状态"→注入提炼（goroutine/线程对比）
- 偶发 JSON 解析失败 → 降级原文拼接（不阻塞判分）

**pairwise 生成层对比（8 题）**：

| 阶段 | 注入更优 | 相当 | 不注入更优 |
|---|---|---|---|
| 第一阶段（原文拼接） | 50%（4/8） | 37.5%（3/8） | 12.5%（1/8） |
| **第二阶段（自评+精炼）** | **87.5%（7/8）** | 12.5%（1/8） | **0%** |

**结论**：相关性自评 + 精炼显著提升注入质量（胜率 50%→87.5%，落败清零）；误命中在注入前被拦截，语义相关命中被保留并精炼。

---

## 增补：卡码全分类扩展抓取（2026-08-10）

**抓取**：java 65 页 / base 71 页 / llm 78 页（llm 为多级路径 `llm/intro|app|...`，脚本已支持）→ 知识库 **2394 块**（base 514 / java 560 / llm 1093 / go 172 / harness 88），version=5。

**主题命中验证**（检索 top1 全部精准）：
- Redis 持久化 → `base/redis-persistence-rdb-aof`
- HashMap put → `java/hashmap-put`
- TCP 三次握手 → `base/tcp-three-way-handshake`
- JVM 内存结构 → `java/jvm-memory-structure`
- 缓存穿透/击穿/雪崩 → `base/redis-cache-stampeding-*`

**负样本语义变化**：知识库覆盖题库主题后，Redis/MySQL/Java 题命中对应主题块是**正确行为**（此前"命中即误命中"定义已过时）；抽查 10 例 9 例主题相关，仅 1 例（Explain type→llm）为语义兜底——由第二阶段自评拦截兜底。

**正样本**：Go 题 Hit@5 98.4%（知识库扩大后仍高），MRR 0.904。

---

## 增补：语义切块重做（2026-08-10）

**问题**：旧切块按 500-800 字纯长度切分，把卡码结构化内容（`## 简要回答/详细回答/知识扩展/追问 Q&A`）切碎成混合块。

**方案**：`split_chunks` 重构为语义切块——按 `##` 标题切分语义单元（小节独立成块、块首保留小节标题、嵌套 ## 自动独立、长节节内切、空节跳过、无 ## 结构退化段落切）。

**效果**（GMP 页）：碎块 → 4 个语义单元（问题/详细回答/work-stealing 机制/追问 Q&A）。
**检索评估对比**（Go 40 题正样本）：

| 配置 | 旧切块 | 语义切块 |
|---|---|---|
| Hit@5 | 98.4% | **100%** |
| MRR@5 | 0.904 | **0.974** |
| Precision@5 | 0.618 | **0.641** |

全指标提升；知识库 2394 → 2303 块（混合块/空节消除）。`--rebuild` 参数支持清空重导（切块策略变更时用）。
