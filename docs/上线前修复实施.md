# 上线前修复计划实施方案（2026-08-10）

> 状态：**已全部实施，328 测试全绿**（`uv run pytest` 328 passed + `node --check` OK；基线 321 + 新增 7）
> 依据：`docs/上线前修复计划.md` 审计清单（B1 busy_timeout / B2 TZ=Asia/Shanghai 已在「已知问题修复方案」中完成，本方案不再列）
> 决策补充：B3 旧 token 迁移策略 = 迁移时回填 `created_at + 30 天`（幂等迁移一次性生效）；引入消息队列——不需要（单进程任务分发已够，见讨论记录，多实例化时随 Redis 一并引入）

## 批次 A —— 上线必办

| # | 问题 | 改动 |
|---|---|---|
| A1 | 部署包含真实密钥 | `docs/服务器运维清单.md` §4 tar 命令加 `--exclude=.env`；部署时由服务器 `.env.bak` 恢复 |
| A2 | 上传解析并发无上限 → MinerU OOM | `routes.py` 模块级 `_parse_semaphore = threading.Semaphore(1)`，`_start_binary_upload.run()` 内 `with _parse_semaphore:` 包住 `extract_text`（只锁解析，LLM 生成不排队） |
| A3 | 判分参考检索跨用户泄漏 | `_reference_for` 加 `user_id` 参数，查询加 `Session.user_id == user_id`；三处调用点（routes.py 1089/1107/1115）传 `row.user_id` |

## 批次 B —— 安全加固

| # | 问题 | 改动 |
|---|---|---|
| B3 | token 永不过期 + user_tokens 无限增长 | `models.py` UserToken 加 `expires_at`；`db.py` 幂等迁移 `_migrate_token_expires_at`（ALTER ADD + 旧行回填 created_at+30d，注册 init_db）；`auth.py` create_token 存 now+30d、user_from_token 过期删行返回 None |
| B4 | 并发注册竞态（双 owner/超名额） | `routes.py` 模块级 `_register_lock`，auth_register 的查重+owner 判定+名额+建用户+commit 整体包锁 |
| B5 | 导出按钮裸链接 401 | `app.js` `#export-btn` click 拦截 → fetch blob（带 Authorization）→ 下载；401 复用统一过期处理 |
| B6 | 复习卷/上传页跨用户残留 | `app.js` 抽 `resetPerUserState()`（reviewRecommended/review-paper DOM/upload-candidates/resumeToken/bankPage/rangeFrom/rangeTo/editingQuestion/question-modal），showAuth 与登录成功处调用 |
| B7 | resume 候选 token 不绑定用户 | `_start_resume_parse` entry 记 `user_id`（调用链 upload_questions→`_start_binary_upload`→`_start_resume_parse` 传入）；`upload_confirm` 先校验后 pop，不符 404 |
| B8 | import 不校验 users 表 | `import_backup` required 表集合加 `users/user_tokens/user_picks`，缺失 → 400「备份不含用户数据，拒绝导入」 |

## 批次 D —— 低风险精选

| # | 问题 | 改动 |
|---|---|---|
| D1 | 无登出 UI | `index.html` sidebar 加「退出登录」；`app.js` handler：logout → 清 token → resetPerUserState + showAuth |
| D2 | `/api/questions/{id}` 匿名可读 | 加 `Depends(require_user)` |
| D3 | 临时目录/uploads 泄漏 | `parsers.py` `_convert_doc_to_docx` 改收 `out_dir` 参数，`.doc` 分支 `TemporaryDirectory` 管理；`routes.py` `_import_facejing`/`_import_resume` 读后删文件 |

## 测试计划（新增 7 个，全部通过）

- A2：并发两个二进制上传 → fake extract_text 记录最大并发数 == 1
- A3：用户 A 高分 judgment 后用户 B 作答 → FakeLLM.calls 断言 B 判分 prompt 不含 A 的回答
- B3：过期 token 401 + 过期行惰性删除；未过期正常放行（test_auth.py）
- B4：双线程并发注册 → 仅 1 个 owner
- B7：他人 token confirm 拒绝（404）
- B8：无 users 表 zip → 400（复用导入测试构造）

## 验证

- 每批：`uv run pytest`（321 基线）+ `node --check app/web/static/app.js`
- 全部完成：重打 deploy 包（**排除 .env**）+ git 提交 + 更新 docs/服务器运维清单.md 部署步骤
