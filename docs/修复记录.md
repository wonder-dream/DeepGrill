## 2026-08-07 18:30

## 地基审查修复记录（M1-M3 跨模块，🟡#1）

> 依据：docs/代码审查.md（2026-08-07 18:24，🟡1 建议本轮修复）
> 验证：修复后 `uv run pytest` → **47 passed**（新增 1 个回归测试）

### 修复：SQLite 外键未强制（🟡#1）

- 位置：`app/db.py` `init_db`
- 问题：SQLite 默认 `PRAGMA foreign_keys=OFF`，伪造 FK 的孤儿行（session_id=99999 的 Attempt）可成功入库（已实证），DB 层对数据完整性无兜底
- 修复：`init_db` 对 sqlite 引擎注册 `event.listen(_engine, "connect", _enable_sqlite_foreign_keys)`，逐连接执行 `PRAGMA foreign_keys=ON`；PRAGMA 是连接级设置，须在每次新建连接时执行
- 复现测试（先写后修）：`tests/test_models.py::test_foreign_key_enforced` —— 伪造 session_id 提交 → `StorageError`（commit 包装 IntegrityError）

### 影响面确认（FK 开启后全量回归）

- 级联删除：ORM delete-orphan 先删子行再删 Session，不受 FK 影响（test_cascade_delete_session 通过）
- 删 Source 无 Questions 场景：test_crud_roundtrip 通过
- 其余插入路径均先建父行拿 id：47 用例全绿

### 遗留项

- ~~🟡#2 api_key 取值收敛~~ → 已于下方修复
- 🟢 项 3-7（choices 兜底/测试 env 清理/_FrozenModel 复用/difficulty 范围/锁版本）延期

---

## 2026-08-07 18:40

## 补充修复：api_key 取值收敛（🟡#2）

> 依据：docs/代码审查.md 🟡#2；用户确认调用方确定（M8/M12/M14 均需构造 LLMClient），YAGNI 不适用，立即修复
> 验证：修复后 `uv run pytest` → **49 passed**（新增 2 个测试）

### 修复：新增 `secret_value(env_name)` helper

- 位置：`app/config.py`
- 形态：`secret_value(env_name: str) -> str` —— 从 os.environ 读值，未设置/空串抛 `EnvVarMissing`（须先经 `load_config` 加载 .env）
- 同步重构：`load_config` 内的 `_require_env` 删除，校验逻辑复用 `secret_value`（一处实现，加载校验与取值共用，不再重复）
- 测试：`tests/test_config.py` 新增 `test_secret_value_returns_env_value`（happy）/ `test_secret_value_missing_raises`（fail + name 断言）
- 待 M8 使用：`LLMClient(model, cfg.llm.base_url, secret_value(cfg.llm.api_key_env))`

---

## 2026-08-07 22:20

## 修复记录：M8 非 str stem 产出垃圾题干（🟡#1）

> 依据：docs/代码审查.md（2026-08-07 22:10，M8/M9 审查 🟡1）
> 验证：修复后 uv run pytest → **138 passed**（新增 1 个回归测试）

### 修复方案

| 项 | 内容 |
|---|---|
| 位置 | `app/pipeline/generate.py` `_validate_questions` |
| 问题 | str(item.get("stem", "")) 将 list/数字/None 等错误类型强转成垃圾题干（实证 ["不是字符串"] → "['不是字符串']"）入库，污染题库 |
| 修复 | stem 必须为 str 类型且非空白才保留，否则与非法 type 同路径丢弃 |

### 变更

- `_validate_questions`：`raw_stem = item.get("stem"); if not isinstance(raw_stem, str) or not raw_stem.strip(): continue`
- 回归测试：`tests/test_generate.py::test_non_str_stem_entries_dropped` —— list/int/None 等错误类型全部丢弃

### 遗留

- 🟡#2 批内重复缺口：已记入 M12 实现清单（已接受新题累积进 dedup existing 池）
- 🟢 项 3-6 延期（logger 死代码/prompt 截断与 tags 上限/异常捕获范围/claimed 缓存）

---

## 2026-08-08 00:07

## 修复记录：GitHub 源克隆目录嵌套（生产环境真 bug）

> 发现场景：真实运行「立即更新」后 data/repos 出现 data/repos/data/repos 嵌套，面经源入库 0
> 验证：修复后 uv run pytest → **199 passed**（新增 1 个回归测试）；真实采集 111 个源入库成功

### 问题

- 位置：`app/crawler/github.py` `_ensure_repo`
- 根因：`_git(cache_dir, "clone", ..., str(repo_path))` 以 cache_dir 为 git 工作目录，而 `repo_path`（如 data/repos/Zchary1106__agent-interview-hub）是**相对路径**——git 将目标相对于自身 cwd 解析 → 克隆进 data/repos/data/repos/... 双重嵌套
- 为何测试未捕获：测试用 `tmp_path`（绝对路径），git 对绝对目标不受 cwd 影响；生产用相对路径 data/repos 即触发
- 后果：_import_repo 按未嵌套的 repo_path 遍历找不到文件 → 入库 0；下次运行 .git 仍不存在 → 重复克隆失败 → 源永久静默失效

### 修复

- `repo_path = (cache_dir / repo.replace("/", "__")).resolve()` —— 克隆目标转绝对路径，与 cwd 解耦
- 回归测试：`tests/test_github.py::test_relative_cache_dir_no_nesting` —— monkeypatch.chdir + 相对 cache_dir（生产形态），断言仓库落位正确且无嵌套

### 前端联动改进（同批）

- `app/web/static/app.js`：点「立即更新」后持续轮询 /api/today（最长 20 分钟，按钮显示已运行分钟），有题自动刷新 + 通知——首次同步需真实 LLM 生成 36 题（10-20 分钟一次性成本），原实现只等 3 秒必然看到空

### 已知外部状态

- 牛客源：接口改版（/discuss 301 到首页，新版 SPA 列表走 JS/XHR），当前源被隔离降级；修复需从浏览器 DevTools 抓真实列表/详情 XHR 样本后更新 M6 常量与解析器（解析层独立，单点修改）

---

## 2026-08-08 00:45

## 修复记录：首次同步零题目（两个真实集成 bug）

> 现象：首次「立即更新」完成后 questions=0、无 generate 错误
> 验证：uv run pytest → **200 passed**；真实 DeepSeek 冒烟单源生成 5 题成功

### Bug 1：生成阶段只处理"本次新采集"的源

- 位置：`app/pipeline/daily.py` `_generate_phase`
- 问题：生成循环 or source in collected——上次导入成功但生成中断（首次同步被杀）后，本轮 collect 返回 0 新源 → 生成空转，111 个已入库源永不生成
- 修复：生成池 = 本次新采集 + 库内**未生成过题目**的全部源（DESIGN 3.2"取未生成过题目的新面经"）；无有效问题的源标记 done 防重复生成
- 回归测试：	est_generates_from_sources_without_questions_on_rerun

### Bug 2：M3 拒绝数组输出，M8 全链路必败

- 位置：`app/llm/llm_client.py` `_parse_json`
- 问题：_parse_json 硬性要求 dict（"not an object"），而 M8 的 LLM 契约是 **JSON 数组**——真实 DeepSeek 按 prompt 返回数组即被拒，每个源生成必失败（FakeLLM 绕过解析，测试未覆盖真实客户端路径）
- 修复：M3 原样返回解析结果（dict/list 均可），类型校验交由调用方（M8 校验 list，M9/M10 校验 dict——均已存在）
- 测试更新：`test_schema_mode_with_array_json_returns_list`（替换原"非对象抛错"用例）
- 真实冒烟：单源生成 5 题（knowledge/design，含 criteria）

### 说明

两 bug 叠加导致：采集成功（111 源入库）→ 生成全静默失败 → 0 题。修复后首次同步需 10-20 分钟（36 题 × 真实 LLM），之后增量。

---

## 2026-08-08 01:10

## 修复记录：答题页点不进去（前端 bug）

> 现象：今日题目正常展示，点击卡片无反应（JS 静默崩溃）
> 验证：后端接口实测全通（today 200 / create session 200 / question detail 200）；修复后浏览器强刷即可答题

### 问题

- 位置：`app/web/static/app.js` `startAnswer`/`renderAnswer`
- 根因：startAnswer 直接把今日列表 item 当作答题页数据渲染，但 /api/today **不返回 good_criteria/bad_criteria**（详情在 /api/questions/{id}）→ q.good_criteria.map(...) 对 undefined 调用 → TypeError → show("answer") 未执行 → 点击无反应且无报错提示
- 修复：进入答题页前先 GET /api/questions/{id} 取详情，合并进 state.question 再渲染
- 注意：浏览器需 Ctrl+F5 强刷（app.js 可能被缓存 304）

### 使用要点（今日确认）

- 首次同步：点「立即更新」后 10-20 分钟（36 题 × 真实 LLM），按钮显示运行分钟，完成后自动出题
- 牛客源当前因改版降级跳过；GitHub 网络不通时单仓库跳过，已入库源不受影响（生成池含库内未生成源）

---

## 2026-08-08 16:10

## 修复记录：追问链面试官提问未展示（真实使用暴露的 UX 缺口）

> 现象：真实作答第一题后页面只显示"追问进行中（已答 1 轮）…"，看不到追问内容、不知道在哪里继续回答
> 验证：修复后 uv run pytest → **226 passed**（更新 1 个断言）

### 问题

- 位置：`app/web/routes.py` `session_status` + `app/web/static/app.js` `pollResult`
- 根因：M11 追问链每轮的面试官提问已生成并落库（`attempts.feedback_text`），但 `/api/sessions/{id}` 的 active 分支只返回 `rounds_done`，**没有透传最新一轮追问消息**；前端自然无处展示 → 用户答完第一轮后看不到面试官在问什么
- 后果：追问链功能形同虚设（用户无法参与多轮对话），仅凭题干反复作答

### 修复

- `routes.py`：`session_status` active 分支新增 `followup` 字段（`_latest_followup` helper：取该会话最新 attempt 的 feedback_text，无则 None）
- `index.html`：答题页题干下方新增 `<p id="answer-followup">`
- `app.js`：`pollResult` active 分支——有 followup 时显示"面试官追问：{followup}"、状态栏提示"请在下方回答追问"；`renderAnswer` 进入新题时清空
- 回归测试：`tests/test_routes.py::test_chain_multi_rounds` 断言第 1 轮后 `followup == "追问：谈谈扩容"`

### 使用要点

- 追问链流程：提交回答 → 显示面试官追问 → 在同一个输入框继续作答 → 循环直到面试官 finish（或 20 轮上限）→ 整轮判分

---

## 2026-08-08 16:40

## 修复记录：追问链对话历史 + 自动清空（方案 B，承接上方修复）

> 现象：追问透传后仍须手动删除输入框旧内容才能答新问题，且看不到多轮对话上下文
> 验证：uv run pytest → **226 passed**（更新 1 个断言）；方案见 docs/对话历史方案.md

### 改动

- `routes.py`：`session_status` active 分支新增 `transcript` 字段（复用 `_attempts_transcript`：`[{role, content}]` 扁平序列）
- `index.html`：答题页 `#answer-followup` 替换为 `<div id="answer-chat">`（对话时间线容器）
- `app.js`：新增 `renderChat(transcript, followup)`——user 灰底"你：…"、interviewer 橙底"面试官：…"，最后一条追问高亮 `.chat-latest`；`pollResult` active 分支渲染时间线 + **自动清空输入框** + 状态栏提示"请在下方回答面试官追问"
- `style.css`：`.chat-item` / `.chat-user` / `.chat-interviewer` / `.chat-latest`
- 回归测试：`test_chain_multi_rounds` 断言第 1 轮后 transcript 含 2 条（user + interviewer）；transcript 仅 active 状态透传（done 后结果页展示判分）

### 使用要点

- 追问链：答题 → 时间线气泡出现（你/面试官）→ 输入框自动清空 → 直接答追问 → … → finish 判分

---

## 2026-08-08 17:20

## 修复记录：追问中会话刷新后"丢失"（D18 恢复流程接线）

> 现象：回答一轮后处在追问环节的题目，刷新后再点就从头开始（上一轮问答不可见）
> 验证：uv run pytest → **233 passed**（新增 3 个测试）；方案见 docs/会话恢复方案.md

### 问题

- 位置：`app/web/routes.py` `create_session` + `app/web/static/app.js` `startAnswer`
- 根因：D18「断点恢复」设计从未接线——`POST /api/sessions` 不检查已有 active 会话，前端 `startAnswer` 永远新建会话且从不调用 resume 接口；数据都在 attempts 表，是恢复流程断了

### 修复

- `create_session`：该题已有 active 会话 → 直接返回它（`resumed: true`），不重复创建；无则新建（`resumed: false`）
- `startAnswer`：优先复用 `q.active_session_id`（resume），拉取 active 会话的 transcript → `renderChat` 恢复完整时间线（含 L 徽标）→ 输入框清空继续答
- 今日列表：`active_session_id && !done` → "**进行中**"徽标（可点击继续）
- 回归测试：重复创建返回同一会话且表数量不变 / 无 active 正常新建 / today 列表带 active_session_id

### 使用要点

- 答一轮 → 刷新 → 题目显示"进行中"→ 点击 → 时间线恢复 → 继续作答
- DESIGN D18 已标记前端入口落地

---

## 2026-08-08 17:45

## 修复记录：空会话堆积（第一题"十几个空回答"）

> 现象：第一题历史详情显示十几个"第 N 次 未完成"的空白尝试，观感像空回答刷屏
> 验证：uv run pytest → **239 passed**（新增 3 个测试）；真实库 29 → 3 会话；方案见 docs/空会话清理方案.md

### 问题

- 位置：`app/db.py`（迁移）+ `app/web/routes.py` `question_history`
- 根因：修复"每次点击建会话"行为之前，每点一次题目卡片就新建一个会话（首题 17 个、全库 26 个）；这些会话无回答（attempts）无判分（judgments）——空壳。attempts 表本身无空记录（空/过短回答提交时已被 400 拦截）

### 修复

- `_migrate_cleanup_empty_sessions`：启动时幂等删除既无 attempts 也无 judgments 的空壳会话（只删无子行，不受 FK 影响；日常新产生空壳自动清）
- `question_history`：attempts 列表跳过 transcript 为空且无判分的会话（展示层兜底）
- 回归测试：迁移清理+幂等 / 空壳不显示 / 答过 1 轮的未完成会话照常显示

### 效果

- 真实库：sessions 29 → 3；第一题 17 → 3（均为真实作答），详情页只剩真实尝试

---

## 2026-08-08 23:00

## 修复记录：旧题标签迁移（LLM 词表重打）

> 背景：词表落地前生成的 151 题含 297 个词表外旧标签（多模态/Netty/RNN 等），不参与分类筛选与复习聚合；人工映射不可行
> 验证：151/151 成功，词表外残留 0（21 → 1 无分类匹配，剩余 1 题为无标签题）；方案见 docs/旧题标签迁移方案.md

### 做法

- `scripts/migrate_tags.py`（一次性数据修复，可复跑）：找出含词表外标签的题 → 逐题 LLM（便宜生成模型）从 58 词词表重打标签 → 代码层词表过滤（上限 5）→ 更新入库
- 单题失败跳过记录，幂等（重复执行无变更）
- weak_tags 历史判分不迁移（复习入口只聚合词表内词）

### 效果

- 词表外标签残留 0；分类覆盖：LLM 285 / Agent 222 / 通用 229 / 后端 114 / 训练 100 / RAG 99
- 遗留：1 题无标签（生成时即空，不参与分类，属合理）

---

## 2026-08-08 23:10

## 修复记录：历史记录分类筛选失效

> 现象：今日/复习页分类筛选正常，历史页选分类后列表变空（"分类失效"）
> 验证：uv run pytest → **251 passed**（更新 1 个断言）

### 根因

- 位置：`app/web/routes.py` `/api/history` item 构造
- `/api/history` 的 item **缺少 `tags` 字段**（今日/复习接口均有）；前端分类筛选依赖 `i.tags`，undefined → 空数组 → 选任何分类全部被过滤

### 修复

- history item 补 `"tags": q.tags`（一行）
- 回归测试：test_history_list 断言 item 含 tags

---

## 2026-08-08 23:30

## 修复记录：D16 严格截断 + torch GPU 版锁定

> 背景：单轮实际入库 40-54 题超 36 上限（整批追加后检查）；requirements.lock pin 了 PyPI CPU 版 torch，sync 会覆盖 GPU 版
> 验证：uv run pytest → **253 passed**（新增 2 个测试）

### D16 严格截断

- `app/pipeline/daily.py` `_generate_phase`：`kept = kept[: max_new - accepted]`——累计入库不超过上限
- 初始放宽：config `max_new_questions` 注释说明"清积压可临时调大再改回"（配置手调，不加开关）
- 测试：额度不足截断（4 题额度 3 → 入库 3）；多源累计不超上限

### torch GPU 版锁定

- `requirements.txt`：`torch==2.6.0+cu124`（pin GPU 版）
- lock 重新 compile（`--extra-index-url https://download.pytorch.org/whl/cu124`）→ `torch==2.6.0+cu124` 与真实环境一致
- README 补 GPU torch 安装命令

---

## 2026-08-09 00:35

## 批量清理积压源（一次性，scripts/bootstrap_generate.py）

> 背景：196 个历史积压源（190 个有有效内容）按每日 36 上限需多轮消化；用户要求直接清理
> 做法：临时放大 max_new_questions（model_copy）复用 run_daily 全套逻辑（生成→去重→入库→选题），sources 传空不触发采集
> 结果：**入库 1633 题**（总题数 556 → 2189，embedding 100% 覆盖），剩余无题源 196 → 15（6 空内容 + 9 LLM 解析失败，后者每日运行自动重试）；耗时约 6 分钟

---

---

## 2026-08-09

## 修复：今日题目包含昨天的题

> 现象：今日列表仍展示昨天选中的题（未完成时 status 一直为 today，list_today_questions 按 status=today 全查）
> 验证：uv run pytest → **265 passed**（新增 3 个测试）

### 根因

- `app/db.py` `list_today_questions` 只按 status == today 查询，无日期边界；昨天标记 today 未完成的题今日仍出现
- 每日选题（`pick_questions`）只从 pending 池取题追加，从不回收昨日遗留的 today 题

### 修复（两处配合）

- `list_today_questions`：加 selected_at >= 本地今天零点 过滤——今日列表只含今天选中的题
- 新增 `recycle_stale_today(session)`：把昨日及更早 selected_at 且**无 finished 会话**的 today 题重置回 pending 池（不丢题，可再次被选中）；有 finished 会话（已完成）的题保持 today 不回池，历史页仍按 selected_at 展示
- `app/pipeline/daily.py` `_pick_phase` 选题前调用回收

### 测试

- `test_today_excludes_yesterday`：/api/today 不含昨日题
- `test_stale_today_recycled_to_pending`：未完成回收、已完成不回收
- `test_stale_today_repicked_on_next_run`：回收后当次选题重新选中昨日未做题

---

## 2026-08-09

## 难度细化 1-3 → 1-5 + LLM 标注

> 背景：难度分级 1-3 过粗；存量 2225 题（1:362 / 2:1254 / 3:609）无法机械映射，须 LLM 重标
> 验证：uv run pytest → **265 passed**；方案见 docs/难度细化方案.md

### 改动

- 新增 `app/difficulty.py`：档位定义 `DIFFICULTY_SCALE_TEXT`（五档 + 示例 + 四维判定）+ `DIFFICULTY_NAMES`，generate/judge/migrate 三处复用
- `app/pipeline/generate.py`：prompt 嵌入档位定义（difficulty 1-5）；`_clamp_difficulty` 钳制 1-5
- `app/judge/judge.py`：难度行带档位名 + 按难度校准判分期望；`PROMPT_VERSION` → judge_v5
- `app/web/routes.py`：直接模式后台补难度（与补标签同一次调用，钳制 1-5）
- `scripts/migrate_difficulty.py`：存量全量重标（可复跑，失败跳过保留原值）
- 前端：`difficultyStars(n)` 星级显示（5 处渲染）；清理死代码 `#detail-difficulty`

### 迁移执行（2026-08-09 12:51，DeepSeek 生成模型）

> `uv run python scripts/migrate_difficulty.py` → **成功 2225 / 失败 0 / 跳过 0**

| 难度 | 迁移前 | 迁移后 |
|---|---|---|
| 1 | 362 | 100 |
| 2 | 1254 | 509 |
| 3 | 609 | 750 |
| 4 | 0 | 768 |
| 5 | 0 | 98 |

分布合理（3-4 级为主，两头少）；此后新题由生成/上传流程 LLM 直接标 1-5。

---

## 2026-08-09

## 题库清洗与自然化改写（scripts/polish_questions.py）

> 背景：题库混入面试者反问（"贵公司主要做什么业务"）、闲聊/流程题（"还有什么想问的"），以及口语化/含糊表述
> 验证：反问/闲聊关键词残留归零；方案见 docs/题目清洗方案.md

- 全量 2225 题 LLM 分类（keep/rewrite/delete，失败保守 keep）→ **删除 37 题**（2225 → 2188，备份 `data/backup/polish_deleted_20260809_134948.json` 可追溯）→ rewrite 类 LLM 改写题干（保持语义，句式自然化，一问一题）
- 残留 3 条口语化题（分类漏网）手动改写：1318（Jmeter 压测）/ 1922（压测数据）/ 1522（工具数量与选择效果）
- 生成侧防再犯：GENERATE_PROMPT 第 5 条补「面试者反问」显式排除
- 多问号 303 条抽查：多为「A是什么？B怎么做？」自然复合问，保留

---

## 2026-08-09

## 数据备份：导出/导入（整库 zip）

- `GET /api/export`：sqlite3 backup API 复制整库 → zip（interview.db + meta.json）下载
- `POST /api/import`：zip 校验（SQLite 魔数 + 必要表）→ 当前库自动备份到 data/backup/pre_import_* → dispose 引擎 → 原子替换 → init_db 重建
- 上传页新增「数据备份」卡片（导出直链 + 导入 zip + 覆盖确认）
- 修 `init_db` 重建时先 dispose 旧引擎（旧连接句柄锁文件，Windows 替换失败）
- 测试：导出 zip 可解压可读 / 导入恢复题数变化 / 无效 zip 与缺表 400（284 全绿）
