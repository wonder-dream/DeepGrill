# 上线前审计与修复计划（2026-08-10）

> 状态：**已审计、计划已定，修复未执行**（用户退出会话，交接下个会话执行）
> 背景：5 批优化全部完成（317 tests 全绿），部署上线前对「用户间隔离 + 部署并发安全 + 前端配合」做了三轮并行审计

## 1. 决策记录（用户已确认）

- 时区：容器设 **TZ=Asia/Shanghai**（「今日」选题/每日流水线按中国时间）
- 修复范围：**全部执行**（必办 5 + 安全 3 + 体验 2 + 低风险精选 3）
- HTTPS 明文传输风险：**后续单独规划**，本轮不做
- 已跳过不做的低风险项：限速 X-Forwarded-For（加反代时再做）、多 worker 防呆（workers 1 已 pin）、复习卷 paper_html 消毒、picks 唯一约束、_upload_tasks GC

## 2. 审计风险清单

### 🔴 高风险（批次 A：上线必办）

| # | 问题 | 位置 |
|---|---|---|
| A1 | **部署包含真实密钥**：`deploy_package.tar.gz` 把 `.env`（真实 API key + 牛客 cookie）打进去了 | 打包命令 / docs/服务器运维清单.md |
| A2 | **上传并发无上限 → MinerU 进程堆积 OOM**：每次 PDF 上传起一个 MinerU subprocess（数 GB 内存），20 用户并发必挂 4C8G | app/web/routes.py `_start_binary_upload` / app/parsers.py:40 |
| A3 | **判分参考检索跨用户泄漏**：`_reference_for`（routes.py:1124-1131）检索**所有用户**高分真实回答注入他人判分 prompt，经 reference_answer/review 回显——用户 B 可读到 A 的简历/项目回答，且 A 回答被发给第三方 LLM | routes.py:1118-1138、1077/1095/1103、1633-1643 |

### 🟠 中风险（批次 B/C）

| # | 问题 | 位置 |
|---|---|---|
| B1 | SQLite 无 busy_timeout：WAL 下并发写仍会 `database is locked` → 500 | app/db.py 连接事件（_enable_sqlite_wal 区） |
| B2 | 时区错位：容器 UTC，「今日」当地 19:00 就翻页、每日流水线当地凌晨 3 点跑 | docker-compose.yml |
| B3 | token 永不过期 + user_tokens 表无限增长 | app/auth.py:63-73 / models.py UserToken |
| B4 | 并发注册竞态：双 owner / 超 20 名额 | routes.py auth_register（208-214） |
| B5 | 导出按钮是裸 `<a href="/api/export">`：不带 token → **导出功能实际不可用**（401） | index.html:84 / app.js |
| B6 | 复习卷跨用户残留：A 的讲义/推荐在 B 登录后仍可见（showAuth 不清理 DOM） | app.js:25、553-613、647-682、showAuth 1381-1390 |
| B7 | resume 候选 token 不绑定用户：B 可确认入库 A 的候选 | routes.py upload_confirm（1035-1048）/ _start_resume_parse |
| B8 | import 不校验 users 表：误导入旧单用户备份 → 全员账号失效 + 权限重置（下个注册者成 owner） | routes.py import required 表集合（923-925） |

### 🟡 低风险（批次 D：精选 3 项）

- **D1 无登出 UI**：sidebar 加「退出登录」（/api/auth/logout + 清 token + showAuth）；当前登出只能靠 401 被动触发
- **D2 `/api/questions/{id}` 匿名可读**（routes.py:320-321 无 require_user）——加鉴权，与其余接口一致
- **D3 临时目录/uploads 泄漏**：parsers.py `_convert_doc_to_docx` 用 `tempfile.mkdtemp` 不清理 → 改 `TemporaryDirectory`；`_import_facejing`/`_import_resume` 写 data/uploads 后不清理 → 读后删

其余低风险（不做）：注册用户名枚举/登录时序差异（限速已缓解）、/api/export 含全用户数据（设计行为）、`_inflight` 无锁读、picks 重复竞态、_upload_tasks 终态不清理、复习卷 innerHTML 自 XSS 面、import 窗口期并发写失败

### ✅ 审计确认安全项（回归防回归）
- 会话/历史/删除全链路 per-user 404 越权拦截（routes.py:425/450/464/476/510）
- done 标志 / weak_tags 聚合 / 复习卷缓存 key 全部 per-user
- owner 路由（上传/题库管理/流水线/备份）require_owner 全覆盖
- 密码 scrypt、token secrets.token_hex(32)、DB 存 sha256
- WAL 生效、迁移幂等、备份 .backup API 一致性、静态目录无敏感面、Windows→Linux 适配、--workers 1 pin

## 3. 修复计划（4 批，接续执行）

### 批次 A：上线必办
1. **A1**：打包命令加 `--exclude=.env`（docs/服务器运维清单.md §4 更新 + 本地重打 deploy_package.tar.gz）
2. **A2**：routes.py 加 `_parse_semaphore = threading.Semaphore(1)`，`_start_binary_upload` 的 run() 内 acquire/release（排队解析防 OOM）
3. **A3**：`_reference_for` 查询加 `Session.user_id == user.id` 过滤（函数签名加 user_id 参数，三处调用点传入）
4. **B1**：db.py 连接事件加 `PRAGMA busy_timeout = 5000`
5. **B5**：app.js `#export-btn` click 拦截 → fetch blob（带 Authorization，401 并入统一处理）→ 下载
6. **B2**：docker-compose.yml services.app.environment 加 `TZ=Asia/Shanghai`

### 批次 B：安全加固
1. **B3**：models.py UserToken 加 `expires_at` 列（幂等迁移 `_migrate_token_expires_at` 注册到 init_db）；auth.py create_token 存 `now+30d`、user_from_token 校验过期（过期删行返回 None）；测试：过期 401 + 惰性清理
2. **B4**：routes.py `_register_lock = threading.Lock()` 包住 auth_register 的 owner 判定 + 名额检查
3. **B7**：`_start_resume_parse` entry 记 user_id；upload_confirm 校验 `entry["user_id"] == user.id` 否则 403/404；测试：他人 token confirm 拒绝

### 批次 C：体验/健壮
1. **B6**：showAuth() 与登录成功处重置前端状态：`state.reviewRecommended`、`#review-paper` innerHTML、`#upload-candidates` 隐藏、`state.resumeToken`、`state.todayDate`、`state.bankPage`、`state.rangeFrom/rangeTo`、`state.editingQuestion` + `#question-modal` 隐藏
2. **B8**：routes.py import 的 required 表集合加 users/user_tokens/user_picks，缺失 → 400「备份不含用户数据，拒绝导入」；测试：旧格式备份拒绝

### 批次 D：低风险精选
1. **D1**：index.html sidebar 加「退出登录」按钮 + app.js handler
2. **D2**：`GET /api/questions/{id}` 加 `Depends(require_user)`
3. **D3**：parsers.py `mkdtemp` → `TemporaryDirectory`；`_import_facejing`/`_import_resume` 读后删文件

### 验证
- 每批：`uv run pytest`（现 317 全绿）+ `node --check app/web/static/app.js`
- 批次 A/B 补测试：参考检索隔离（跨用户不注入）、token 过期、import 拒绝、confirm 越权拒绝
- 全部完成：重打 deploy_package.tar.gz（**排除 .env**）+ git 提交

### 接续顺序
A → B → C → D，逐批提交；最后重打包并同步 docs/服务器运维清单.md。
