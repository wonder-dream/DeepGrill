# 冗余与死代码清理方案

> 状态：执行中（按步骤推进，每步一个 commit，每步冒烟 + 全量回归）
> 日期：2026-09-06
> 基线：`pytest tests/ -q` **403 passed**（commit `52469e1` 之后、清理开始前）
> 冒烟方式：`.pytest_tmp/smoke.py`（gitignore 内，不入库）——`node --check` 两个前端 JS + TestClient 内存库装配应用，校验静态资源 200 / 未登录 API 401

## 背景

2026-09-06 对全仓库（app/ 2.2 万行）做了死代码与冗余审查，方法为逐符号全仓库 grep 交叉引用 + 后端/前端/脚本配置三路独立排查 + 人工复核防误报（FastAPI 路由装饰器、pydantic 字段、动态类名拼接均已排除）。审查结论全文见当日会话报告，本方案是处置计划与实施记录。

## 处理顺序总表

| 步骤 | 内容 | 状态 |
|---|---|---|
| 1 | 安全处置 `deploy_package.tar.gz`（已验证当前包不含 .env，过期部署产物销毁） | ✅ 本 commit |
| 2 | 修复两个坏脚本：`eval_rag_gen.py` 缩进损坏、`collect_nowcoder_days.py` 引用已删除的 `Source.raw_text` | ⏳ |
| 3 | 删除后端确认死代码 6 处 + 前端死函数 3 个 + 死 CSS 约 80 行 + 死变量 2 个 | ✅ commit 4 |
| 4 | 仓库卫生：`git rm --cached` .idea/.opencode、.gitignore 补规则（Kimi 参考稿）、清理旧备份约 125MB | ✅ commit 5 |
| 5 | 决断"仅测试引用"函数（删 3 个、收编 1 个、保留 1 个）+ 删除 `smtp:`/`notification:` 死配置 | ✅ commit 6 |
| 6 | 无用 import 清理 + 低风险重复逻辑合并（可选，按测试覆盖度取舍） | ✅ commit 7 |

每步完成后在本文件追加实施记录与测试证据。

---

## 步骤 1：安全处置过期部署包

### 现场核实

- `docs/上线前修复计划.md` A1 记录：早期版本的 `deploy_package.tar.gz` 曾把真实 `.env`（API key + 牛客 cookie）打进包。
- 解包核验当前根目录这份（2026-08-16 11:30，A1 修复后的重打包）：**成员列表只有 `.env.example`，不含 `.env`，无密钥**。即文档所述泄漏发生在更早的打包版本，当前这份是修复后的产物。

### 处置决定

- 删除根目录 `deploy_package.tar.gz`：内容可随时由 git 仓库 + 打包命令重新生成，属于过期产物，无唯一信息。
- 遗留风险记录：若更早的（含密钥的）打包副本曾外发到其他机器/网盘，仓库侧无法核销，**建议择机轮换 `LLM_API_KEY` 与 `NOWCODER_COOKIE`**。
- 附带核实：`interview_backup_20260813_165500.zip`（26.9MB，8 月 13 日）留待步骤 4 与旧库备份一并处置。

### 实施记录

- 2026-09-06 删除 `deploy_package.tar.gz`（1.3MB）。
- 测试：不涉及代码变更，跳过回归（后续每个代码步骤均跑冒烟 + 全量）。

---

## 步骤 2：修复坏脚本（方案）

两个脚本在 `docs/交接文档.md` / `docs/RAG测评报告.md` 中被列为可用工具，但当前无法运行：

1. `scripts/eval_rag_gen.py:117-119`：`for item in sample:` 循环体前两行丢失缩进（commit `8a8dfaf` 引入），`py_compile` 报 IndentationError。修复为正确缩进；顺带把对 `app.web.routes._knowledge_for` 私有函数的跨层 import 保留不动（修复保持最小，不重构）。
2. `scripts/collect_nowcoder_days.py:47`：`Source(raw_text=...)` 引用已删除字段（`app/models.py` 注明 raw_text 已移除）。按现行模型语义改为只写 `cleaned_text=clean_text(...)`；顺带清掉该文件 3 个无用 import（`timedelta`/`select`/`nowcoder`）。

验收标准：`py_compile` 两脚本通过；`collect_nowcoder_days.py` 对 monkeypatch 数据跑通入库分支（现有测试不覆盖脚本，以最小手工驱动验证）；全量回归 403 passed 不回退。

## 步骤 3：删除确认死代码（方案）

后端（全仓库零引用，grep 复核过）：
- `app/auth.py` `validate_username()` + `USERNAME_RE`
- `app/db.py` `list_questions_by_date()`、`_rank_recommend()` + 仅被其调用的 `_weak_tag_weights()`
- `app/errors.py` `PipelineStepError`
- `app/retrieval.py` `knowledge_search()`（运行时与评估脚本均只用 `knowledge_search_multi`）
- `app/web/routes.py` `_tag_ids_by_name()`

前端（app.js / style.css / theme.css）：
- `app.js` `restoreAnswerSession()`（answer/result 不在 HASHABLE，无调用方）、`restoreDetailSession()`、`bankCardClickGuard`
- `style.css` 1293-1367「参考稿通用组件（备用）」整块 + 1393 行 `#wipe` media 规则 + 错放本文件的 `.rv-danger`/`.rv-toast`/`.rv-modal-card` 规则（这些类只属于 reviewer 页，reviewer.html 不加载 style.css）
- `theme.css` `--input-border`（零引用）、`--muted`（仅被上述死块引用）

验收标准：删除后 `node --check` 通过、冒烟 12 项全 OK、全量回归 403 passed（若死代码被测试引用导致用例失败，说明误判，回滚该项）。

### 实施记录（2026-09-06）

- 后端：`auth.py` 删 `validate_username`+`USERNAME_RE`（`re` 仍被 PASSWORD_RE 使用，保留）；`db.py` 删 `list_questions_by_date`/`_weak_tag_weights`/`_rank_recommend`，连带 `timedelta` 成为孤立 import 一并删；`errors.py` 删 `PipelineStepError`；`retrieval.py` 删 `knowledge_search`；`routes.py` 删 `_tag_ids_by_name`。
- **审查修正**：style.css「备用」块并非全部死代码——删除前逐类 grep 复核发现 `.mono-num`（index.html 3 处）与 `.caret`（app.js classList 动态添加）+ `@keyframes blink` **在用**，予以保留并移出死块单独成组；其余 `.topbar`/`.logo`/`.pill`/`.lift`/`.tag`/`.reveal`/`#wipe`/`.kicker`/`.wrap` 确认零使用删除（grep 命中的 logo/logout、wrap/变量 等均为子串误报）。
- `style.css` 另删：危险色分组选择器中的 `.rv-danger`/`.rv-toast.rv-toast-error` 成员、弹层分组中的 `.rv-modal-card` 成员（index 页无这些类）、body 背景 3 行原样重复（与文件头部 16-18 行重复）、`#wipe` 的 reduced-motion 规则行；同步修正两处提及 #wipe 的注释。
- `theme.css` 删 `--muted`、`--input-border`（死块删除后两者零引用）。
- `app.js` 删 `restoreAnswerSession`（53 行）/`restoreDetailSession`（16 行）/`bankCardClickGuard`（3 行）。
- 测试：CSS 括号平衡 ✓、后端 5 模块 import ✓、`node --check` ✓、冒烟 12 项 ✓、全量回归 **403 passed** ✓。

## 步骤 4：仓库卫生（方案）

- `git rm --cached`：`.idea/` 5 个文件（.gitignore 已有规则但未出索引）、`.opencode/plans/` 3 个计划文档。
- `.gitignore` 追加：`Kimi_Agent_面试平台前端风格/`（本地风格参考稿，无文档引用）。
- 删除 `data/backup/` 中 4 个一次性全库备份（`bak_20260810`、`bak_pre_rebuild_20260811`、`pre_clear_20260812`、`pre_trim_20260810`，约 98MB；5 个 `pre_import_*.db` 由导入接口自动保留机制管理，不动）与根目录 `interview_backup_20260813_165500.zip`（26.9MB）。

### 实施记录（2026-09-06）

- `git rm -r --cached .idea .opencode/plans`：IDE 配置 5 文件 + agent 计划 3 文件出索引（本地文件保留）。
- `.gitignore` 追加：`Kimi_Agent_面试平台前端风格/`、`.opencode/`。
- 删除 4 个一次性全库备份（14.8+32+36+14.8MB）+ 根目录 `interview_backup_20260813_165500.zip`（26.9MB），共释放约 121MB；`data/backup/` 仅剩导入接口自动管理的 `pre_import_*.db`（5×176KB）与删除恢复记录 json（4KB）。
- 测试：冒烟 12 项 ✓、全量回归 **403 passed** ✓（本步无代码变更，按约定照跑）。

## 步骤 5：仅测试引用函数决断 + 死配置（方案）

决断原则：守护死路径的测试连同函数一起删；能收编为运行时 API 的收编；承载独立行为的保留。

| 函数 | 决断 | 理由 |
|---|---|---|
| `db.list_today_questions` | 删（连同 test_models/test_daily 中用例） | `/api/today` 走 `_ensure_today_picks` + UserPick 内联查询 |
| `db.count_questions_created_since` | 删（同上） | D16 上限实际以 `accepted` 计数 |
| `crawler/github.ensure_repos` | 删（连同 test_github 单用例） | 管线只走 `github.collect` |
| `db.latest_task_log` | 收编：`/api/daily/latest` 改用它 | 消除 routes.py 内联同款查询的重复；测试不变 |
| `scheduler.trigger_now` | 保留，仅修正撒谎的 docstring | 承载调度器锁/异常行为，scheduler 测试经它验证；改 `/api/daily/run` 语义属行为变更，不做 |
| `judge.PROMPT_VERSION` | 保留 | 测试为防漂移断言，有意设计 |

死配置：
- `config.yaml` 删 `notification:` 节；`app/config.py` 删 `NotificationConfig`（运行时零消费者，功能从未落地）。
- `config.yaml` 删 `smtp:` 节；`app/config.py` 删 `SMTPConfig` + `AppConfig.smtp` 字段（真实配置在 `email_code.py` 直接读 `os.environ`，yaml 节是影子配置，改了不生效）。
- 同步删 test_config.py 中对应断言；修正 routes.py:497 注释漂移（OWNER_USERNAME → OWNER_EMAIL）。


### 实施记录（2026-09-06）

- 删除：`db.list_today_questions`、`db.count_questions_created_since`、`github.ensure_repos`；test_models 的 `test_count_questions_created_since`、test_github 的 `test_ensure_repos_returns_paths` 随之删除，`test_pick_questions_per_user_pool` 改用与 `/api/today` 同口径的内联 UserPick 查询断言，`test_empty_table_helpers`/`test_bulk_insert`/test_daily 相应用例剔除死调用。
- 收编：`/api/daily/latest` 改用 `db.latest_task_log(session, "daily")`（task_name 仅 "daily" 一种取值，语义等价），消除内联重复查询。
- 保留：`scheduler.trigger_now`（承载调度器锁/异常行为的测试入口），docstring 修正为如实描述（Web 按钮走 BackgroundTasks）。
- 死配置：config.yaml 删 `notification:`/`smtp:` 两节；config.py 删 `NotificationConfig`/`SMTPConfig` 及 `AppConfig.notification/smtp` 字段；test_config 的 VALID_YAML 与断言同步；test_daily/test_scheduler/test_routes 的 make_config 辅助移除 notification 参数。
- 修正 routes.py startup 注释漂移（OWNER_USERNAME → OWNER_EMAIL）。
- 测试：残留引用 grep 清零 ✓、config.yaml 加载 ✓、冒烟 12 项 ✓、全量回归 **401 passed**（403 − 2 个随死代码删除的用例）✓。

## 步骤 6：无用 import + 低风险合并（方案，可选子集）

做：删无用 import（`db.or_`、`models.BLOB`、`daily.QuestionType`、`routes.cast/delete/String`、`crawl_kamacoder.re`）；合并逐行相同的解码函数三连与 `_extract_title` 两份（crawler/github、crawler/importer、parsers，测试覆盖充分）。
不做（记录理由）：质量分类 prompt 三份合并涉及 prompt 行为差异，需独立评测；`_generate_phase`/`generate_source_immediately` 流水线合并涉及测试 FakeLLM 特判语义，收益风险比不划算；CSS 分层覆盖属有意设计，仅删已确认的纯冗余（已在步骤 3 覆盖）。

### 实施记录（2026-09-06）

- 无用 import：`db.or_`、`models.BLOB`、`daily.QuestionType`、`routes.cast/delete/String`、`crawl_kamacoder.re` 逐一 grep 复核后删除（`exists`/`or_`/`text` 在 routes/db 有真实使用，保留）。
- 合并三连解码 + 两份 `_extract_title`：公共实现 `decode_md_txt` / `extract_title` 落在 `app/parsers.py`（叶子模块，crawler 与 web 均可依赖）；`github._decode_text`、`importer._decode` 改为薄包装（翻译 ValueError → 各自模块异常类型），两份逐字相同的 `_extract_title` 删除改 import。行为不变。
- 实施过程注记：parsers.py 重构中曾因 Edit 边界错误短暂破坏 `_extract_with_mineru`/`_convert_doc_to_docx` 函数头，已按原文件全文重写修复；最终 git diff 仅含上述预期变更（函数改名 + 新增公共 extract_title + 调用点更新），`py_compile` 与解析相关 20 个用例验证通过。
- 不做项（理由见方案）：质量分类 prompt 三份合并（需独立评测）、`_generate_phase`/`generate_source_immediately` 流水线合并（涉及测试桩特判语义）、CSS 分层覆盖（有意设计）。
- 测试：py_compile 8 文件 ✓、github/importer/clean/nowcoder 72 用例 ✓、冒烟 12 项 ✓、全量回归 **401 passed** ✓。
