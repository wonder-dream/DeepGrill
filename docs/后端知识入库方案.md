# 后端通用知识入库（Python + Java 双视角）实施方案

> 状态：已确认，实施中 ｜ 日期：2026-08-08

## 1. 目标

让题库覆盖后端通用知识（缓存、并发、设计模式等），Python + Java 双视角全覆盖。走既有链路：**扩词表 → 面经素材手动导入 → LLM 生成题**，不预置直插库。

## 2. 词表扩充（app/tags.py，+8 词 → 58 词）

新增：缓存、并发、锁、IO、Python、GIL、asyncio、FastAPI
（Java/Spring/JVM/MySQL/网络/设计模式/分布式/高并发/消息队列等已有）

## 3. 面经素材 `data/面经-后端通用.md`（8 章，真实面经格式）

1. 缓存与 Redis（语言无关）
2. 并发·Python（GIL/协程/asyncio/锁/队列/线程池）
3. 并发·Java（线程池/锁/volatile/CAS/ConcurrentHashMap/AQS）
4. 设计模式（单例/工厂/策略/观察者/责任链/装饰器，双视角）
5. 存储/MySQL（索引/事务/隔离级别/锁/分库分表）
6. 网络（TCP/HTTP/HTTPS/WebSocket/RESTful）
7. 运行时（Python GC/装饰器/生成器/FastAPI + JVM 内存/GC/类加载）
8. Spring（IoC/AOP/循环依赖/Bean 生命周期）

## 4. 导入

```bash
uv run python -m app import data/面经-后端通用.md --type manual
```

首轮生成 ≤36（D16），剩余「立即更新」自动补全（既有补生成机制）。

## 5. 验证

- source 落库；tags 分布（缓存/并发/设计模式/Python/GIL/Java/JVM/Spring 高覆盖）
- 抽样题目质量；全量测试回归

## 6. 文档

- 本文档；MODULES/M08 词表行更新；README 手动导入示例补该命令

## 明确不做

- 改生成 prompt 题型逻辑；预置题目直插库
