# 交接文档（会话退出快照）

> 日期：2026-08-10 ｜ git 状态：工作区干净，42 个提交全部入库（最新 `b51a8fd`）
> 用途：新会话/新 agent 接手的起点——先读本文件，再按指针读相关文档

## 1. 项目一句话

个人求职准备工具：每日自动采集面经 → LLM 出题 → 深挖追问链答题 → 四维判分 → 薄弱点复习，全本地运行（FastAPI + SQLite + bge-m3 本地向量）。

## 2. 运行方式

```bash
uv run uvicorn app.main:app --host 127.0.0.1 --port 8000   # 启动服务
uv run pytest                                              # 335 个离线测试
uv run python -m app import 面经.md --type manual          # CLI 导入
uv run python scripts/bootstrap_generate.py                # 批量清理积压源（一次性）
uv run python scripts/migrate_tags.py                      # 旧题标签 LLM 重打（一次性）
uv run python scripts/retag_empty_tags.py                  # 空标签/全库补标（可复跑，--all 全库）
uv run python scripts/loadtest.py --token <token> --mode read --concurrency 20 --total 100  # 真并发压测
uv run python scripts/migrate_difficulty.py                # 存量难度 LLM 重标 1-5（一次性，约 20-40 分钟）
uv run python scripts/polish_questions.py                  # 题库清洗（LLM 分类 keep/rewrite/delete，可复跑）
uv run python scripts/eval_retrieval.py                    # 检索评估（hit_rate/MRR）
```

- GPU torch 已装（2.6.0+cu124，requirements.lock 已 pin）；`.env` 需配 `LLM_API_KEY` + `NOWCODER_COOKIE`
- 浏览器用后普通 F5 即可（index.html 已 no-store，静态资源带 `?v=` 版本参数，改前端后须 bump 版本号）
- 数据备份在 Web 上传页（导出 zip / 导入恢复，导入前自动备份当前库）；PDF/DOCX/DOC 上传解析需系统 LibreOffice + mineru 模型（均已装，模型缓存 ~1.2GB 在 HF cache）

## 3. 当前状态

| 项 | 状态 |
|---|---|
| 功能模块 | M1-M14 + Phase 2 + 统计面板（/api/stats 打卡圆点/答题趋势/概览/完成进度，整合右侧日历面板随视图切换）+ 题库分页增强（页码跳转/每页条数 10-50）+ hash 路由状态恢复（刷新/后退保留筛选页码范围标签）+ 答题/详情中间态刷新恢复（sessionStorage）+ 复习卷刷新自动恢复 + 导入备份保持登录态 + 布局 880px + 空状态美化 + 题卡字体统一 15px + 登录回车 + 退出按钮贴底 + 全部安全加固（见修复记录） |
| 测试 | **335 全绿**（全离线，无网络/无真实 LLM） |
| 题库 | 2293 题，embedding 100% 覆盖，分类全覆盖；难度分布 1:100 / 2:509 / 3:750 / 4:768 / 5:98；**0 题源归零**；标签词表 68 个（含 Go/操作系统/Docker/K8s/Git 等新增） |
| 判分数据 | 极少（本地数条测试数据；需真实答题积累，统计面板价值依赖数据） |
| git | 42 个提交，工作区干净；最新 `b51a8fd` |
| 敏感文件 | `.env` / `data/interview.db` / `data/repos/` / `data/uploads/` / `data/backup/` / `data/logs/` / `demos/*.pdf` 均 gitignore |

## 4. 关键文档指针（勿重复阅读全部）

| 主题 | 文档 |
|---|---|
| 总体设计/决策表 | `docs/DESIGN.md`（D1-D18 + Phase 2 §9） |
| 模块总览 | `docs/MODULES.md`（M1-M14 + E1/E2，251→335 测试数已更新） |
| 模块细节 | `docs/modules/M01~M14-*.md` |
| 功能方案 | `docs/*方案.md`（标签词表/语义去重/薄弱点复习/参考检索/深挖追问/分批生成/会话恢复/历史详情/历史删除/分类筛选/题库浏览/上传题目/难度细化/题目清洗/文档解析集成/页面充实/布局重构 等） |
| 修复与状态变化 | `docs/修复记录.md`（含全部 bug 修复与数据迁移记录，最新 5 条 2026-08-10） |
| 本轮专项 | `docs/已知问题修复方案.md`（4 个实际使用 bug）、`docs/上线前修复计划.md` + `docs/上线前修复实施.md`（安全加固 11 项）、`docs/页面充实方案.md` + `docs/布局重构方案.md` |
| 运行说明 | `README.md` |

## 5. 遗留事项 / 下一步建议（按优先级）

1. **服务器部署未执行（首要）**：本轮全部修复（时区 TZ/缓存/502 抗抖/中文标签 json_each/安全加固/统计面板/刷新恢复）仅本地生效，服务器 <服务器IP> 仍是旧代码。部署步骤见 `docs/服务器运维清单.md` §4（打包**必须排除 .env**，A1 教训）；部署后容器内跑 `retag_empty_tags.py` 补标
2. **积累真实判分数据**：判分记录极少，统计面板/复习卷/参考检索的价值依赖答题数据（用户正常使用即可）
3. **可选后续**：薄弱诊断 v3（复习卷结合判分数据）、答题统计/趋势深化、面试模拟模式（多题连问/限时）、手机端适配打磨、首次 embedding 冷启动优化
4. **可选**：`docs/上线前修复计划.md` 审计中标记"不做"的低风险项（X-Forwarded-For 限速、多 worker 防呆、paper_html 消毒、picks 唯一约束）若上线后遇到再评估

## 6. 敏感信息提醒

- API key / cookie 只在 `.env`（已 gitignore）；交接/贴日志时注意脱敏
- 牛客 cookie 过期会导致该源静默停用（日志有 `disabled today`），重新 F12 复制更新 `.env`
- token 30 天过期（B3），过期后需重新登录

## 7. 给接手 agent 的提示

- 项目原则见 `AGENTS.md`（Karpathy 准则：先想后写、简洁、surgical、可追溯）
- 改动后必跑 `uv run pytest` + `node --check app/web/static/app.js`；**改前端静态资源后必须 bump `index.html` 里的 `?v=` 版本号**（否则浏览器命中旧缓存）
- 数据库迁移都在 `app/db.py` 的 `_migrate_*`（启动自动执行，幂等）；`init_db` 重建会先 dispose 旧引擎（备份导入依赖，Windows 文件锁）
- 涉及 LLM 的测试全部用 `tests/fakes.py` 的 Fake 实现，勿在测试中触网
- **勿手动删除 `interview.db-shm/-wal`**（会丢未 checkpoint 写入，2026-08-10 踩过坑）
- **标签查询必须用 `json_each`**（`routes.py` `_tags_exists/_tags_contains`）：SQLAlchemy JSON 序列化用自定义 `JSONUtf8`（中文不转义），旧库中文标签为 `\uXXXX` 存储，`LIKE` 对中文永远匹配不上（历史 bug 根因）
- **Embedder 是进程级单例 + 加载锁**（`routes.py _default_embedder_factory` + `embed.py _load` 双检锁）：勿改回每次新建（4C8G 并发双份 ~2.3GB 会打满）
- 前端路由：主视图 hash 带状态参数（`#bank?page=2&q=缓存`，`updateHash` pushState 写入，刷新/后退可恢复）；答题/结果/详情中间态用 `sessionStorage`（`answer_state`/`detail_state`/`review_paper_tag`）恢复；离开流程/换用户时清除
- 压测：`scripts/loadtest.py`（read 并发安全；answer 模式触发真实 LLM，并发 ≤2，且同用户同题并发会被 409 保护拦下属预期）
- 大功能先做独立 demo（如 `demos/parse_demo.py`）人工审查后再集成（用户要求的工作流）
