"""候选人档案领域（CONTEXT.md）：**从一份简历提取出的结构化经历描述**。

它是**私有题集**与定制**模拟面试**的输入，且**不保存简历原文**（决策 8）。

边界（ADR-0005）：
- 简历文本 → 结构化档案：本领域（要调 LLM，但那是 `llm` 基础设施）
- 由档案生成题：**写题目表的是 `bank`**，所以那一步住在 `offline`（跨领域动作）
- 注销与导出：本领域（数据都属于这个人）
"""
