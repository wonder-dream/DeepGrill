"""知识领域（CONTEXT.md）：管**知识本身** —— 知识点、考察点、命中状态的聚合与**薄弱点**。

它**从不判定"哪道题挂错了"** —— 那是 `offline` 的挂载自修复（跨领域，ADR-0005）。
本领域也不写 `questions` 表：题目挂在哪个知识点下由 `bank` 写，这里只**读**它来
把命中记录归到知识点上。

MVP 的读侧只有一件产品要求的东西：**掌握度矩阵**（决策 24 / 17）。
"""
