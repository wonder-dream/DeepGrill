"""编排层：跨领域动作的固定去处 + 离线任务（ADR-0005 / ADR-0010）。

它有两条职责，都不属于任何单个领域：

- **跨领域的写入**（如 `seed`：同时写题库与知识层）。领域之间不许互相 import，
  所以"同时需要两边"的动作只能住在能依赖两边的那一层。
- 将来的离线管道与 `worker.py`（ADR-0006）。

CONTEXT.md 记着这个别扭感是**边界清晰的代价**：写自修复时会觉得"这明明是知识层
的事却放在 offline"—— 那是设计生效的表现，不是失误。
"""
