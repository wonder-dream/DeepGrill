"""面试报告领域（ADR-0003）：**报告由数据拼装，只有一段总结由 LLM 生成**。

六块各自的来源（这是 ADR-0003 的核心那张表）：

```
① 总分 + 四维分 ──────── 【查】 evaluations
② 掌握度矩阵变化 ─────── 【算】 「本场之前」与「本场之后」的命中率相减
③ 逐题回顾 ──────────── 【查】 report_items 的快照 + evaluations + attempts
④ 该补的前置知识点 ───── 【图】 沿 knowledge_point_edges 往前走一跳
⑤ 主要问题清单 ───────── 【算】 漏得最多的考察点，按知识点归类
⑥ 一段总结 ←─────────── 【唯一的 LLM】
```

**生成时机**：主体与总结都在面试结束时算好并**落库**（`interviews.report_body`
/ `report_summary`）。两条理由都是硬的：

· ② 依赖"本场之前"这个时间点，事后重算可能与当时不同
· 总结不落库的话，每次打开措辞都会变 —— 与"报告不重新生成"直接矛盾

本领域**不写** `questions` / `criteria` / `attempts`，只读它们（写表的只有各自
的领域，ADR-0005）；它写的只有 `report_items` 与那个 `interviews` 行的报告列。
"""
