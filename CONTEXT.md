# DeepGrill

> 状态：活文档（长期维护）｜ 只放术语，不放实现细节、范围陈述、会变的数字。职责与寿命见 `docs/INDEX.md`

## Language

### 产品形态

**模拟面试**:
一场有开头有结尾的面试：AI 面试官连续提问并追问若干道题，结束时给出总的评估报告。
_Avoid_: 答题、练习、套题

**单题追问**:
从题库选一道题，由 AI 面试官追问到底的练习单元。它与**模拟面试**共用同一个面试官，区别只在于编排长度。
_Avoid_: 刷题、做题

**题库练习**:
第三种形态：不进入面试、也没有**编排**，只在**题库**里按岗位与知识点筛题、自己看参考答案与评分标准。它**不消耗额度点**。
_Avoid_: 题库刷题、自助刷题

**编排**:
决定「问哪些题、每道题追问到什么程度、什么时候结束」的那一层，即**控制流的持有者**。三种产品形态的差异**只存在于这一层**，向下共用作答与面试官。面试官整体是一条 **workflow**（带循环的状态机），不是一个自主 Agent —— 分界线在于「否决权在代码手里还是模型手里」。
它在面试过程中会**根据本场已产生的判定结果调整后续题目**（跳过下游、插入前置）。这是「对候选人的判断」的落地形式，但它**不是**一个独立的能力模型。
_Avoid_: 流程、模式、pipeline、工作流引擎、Agent、能力模型

**面试官**:
扮演提问与追问角色的 LLM 能力单元，是**模拟面试**与**单题追问**共用的同一个东西。它必须可拆装：关掉它之后，**题库**仍可独立工作。
_Avoid_: 出题器、judge、评分器

**作答**:
候选人对一个问题的一次回答，无论发生在哪种产品形态下，都是同一种东西。所有产品形态的差别只在编排，不在作答。
_Avoid_: 回答记录、attempt、练习记录

**输入模态**:
一次**作答**是「说的」还是「打的」。它是 v2 新增的变量，且只改变两件事：**clarity** 这一维的含义（打字测组织与排版，语音测表达流畅，ADR-0009），以及命中判定要不要为**转写容错**让路（决策 85）。
_Avoid_: 输入方式、语音开关

**转写容错**:
语音**作答**的话先过一遍 STT，同音/近音的技术术语会被写成别的字（「拉格」实为 RAG、「哈西表」实为哈希表）。判定**命中状态**时按「**语音上说得通**」还原候选人**已经表达**的意思 —— 写错字本身不算他答错，追问里则要写正确的术语。**放宽的只有「字写错了」这一件事**：整段话里没有对应某条考察点的意思时仍记未命中（否则每条考察点都能被猜成命中，**掌握度矩阵**立刻失真）。打字**作答**没有这道口子 —— 那里的错字是他自己敲的。
_Avoid_: 错别字纠正、拼写检查、ASR 后处理、术语归一

### 内容

**题库**:
可被检索和抽题的公共题目集合，是**练习场**，不是产品本身。它的质量责任靠反馈闭环承担。
_Avoid_: 题池、数据集

**私有题集**:
由某位候选人自己的**候选人档案**生成的题目集合，只对该候选人可见、不参与公共检索。
_Avoid_: 我的题库、草稿、暂存

**晋升**:
把一道题从**私有题集**移入**题库**的单向动作，由候选人主动发起、经**内容门禁**后生效。不可逆。
_Avoid_: 投稿、上架、分享、公开

**内容门禁**:
内容进入**题库**所需的自动化关卡集合（质量筛选、去重等），失败时保守放行；人工不再前置介入。
_Avoid_: 审核、reviewed_at、人工审核

**内容来源**:
**题库**中题目的出处，分三类：种子题（从 v1 库全量导入的真实面经题）、生成题（LLM 产出）、晋升题（来自**私有题集**）。三者的可信度预设不同。
_Avoid_: 数据源、爬虫源、crawler

**参考答案**:
一道题随题携带的示范回答，属于**题库**内容而非**面试官**能力：它由面试官在离线富化任务中生成一次、之后读取零成本。不是每道题都有。
_Avoid_: 缓存答案、离线答案、标准答案

**答案档位**:
一道题是否附带**参考答案**的标记。分泛用档（附答案，离线可读）与长尾档（只有评分标准）。档位可随题目被调用的次数从长尾档提升到泛用档。
_Avoid_: 题目等级、优先级、冷热标记

### 知识

**薄弱点**:
候选人在某个**知识点**上**没答到的那些考察点**（知识点 × 考察点的未命中记录）。它是**掌握度矩阵**的直接读数，也是复习推荐的单位 —— 不是标签、不是分数。
_Avoid_: 弱项、短板、weak_tags

**知识点**:
一道题所考察的那个知识单元，是**薄弱点**、重复判定与复习推荐的共同单位。它带一句可验证的定义：考什么、不考什么。
_Avoid_: 标签、考点、tag

**考察点**:
**知识点**定义中「要考察的那几条」的具体条目。判断回答质量与定位**薄弱点**都以是否答到考察点为准，而不是以打分或标签为准。**考察点的顺序就是追问的路线** —— 第 N 条未命中，下一问就朝它去。
_Avoid_: 得分点、评分项、key point

**命中状态**:
每一轮对当前题**全部考察点**的判定结果（命中 / 未命中 / 未涉及），**逐轮累积成快照**。它同时是**追问的驱动信号**、**掌握度矩阵**的唯一来源、以及挂载自修复的依据 —— 取代了 v1 的 `quality`（回答好坏）与 `weak_tags`（薄弱标签）。
_Avoid_: 得分、评分、质量、quality、weak_tags

**知识地图**:
**知识点**之间的层级与依赖结构（哪个领域下有哪些知识点、谁是谁的前置）。它由 LLM 提议、由人确认 —— 因为地图错了会波及整条链上的所有题，而单道题挂错只看得出、也能自动修正。
**它是森林而不是一棵树**：顶层知识领域有多个、彼此并列（Java 并发与 RAG 检索之间没有父子关系），且知识点可被多条路径共享，所以准确说是**有向无环图**。
_Avoid_: 标签体系、分类树、图谱、一棵树

**岗位**:
**目标岗位**（用户的求职方向）之外的另一种含义：平台上的岗位词表，用于表达「某个岗位要考哪些**知识点**」。它是**独立于知识结构的另一条轴**，不是**知识地图**的一层。
_Avoid_: 分类、领域、role 节点

**主知识点**:
一道题挂在**知识地图**上的那一个节点，只决定「这道题住在哪里」以及「它应该命中哪个知识点」。它**不参与掌握度计算**。
_Avoid_: 所属知识点、分类

**关联知识点**:
一道综合题**还牵动**的知识点（一道系统设计题可以牵动五六个）。用于按知识点组卷与能力覆盖检查，同样不参与掌握度计算。
_Avoid_: 次标签、辅助标签

**掌握度矩阵**:
「候选人 × 知识点」的二维视图。每格是该候选人在该**知识点**上**考察点**的命中比例（不是分数）。它是**薄弱点**唯一对用户可见的形式，也是**面试报告**与个人状态页共用的数据结构。它只由考察点命中记录推导，与题目挂在知识地图的哪个节点下无关。
_Avoid_: 热力图、雷达图、能力画像

**面试报告**:
一场**模拟面试**结束后呈现给候选人的复盘材料。它由已落库的数据拼装而成，不是重新生成的叙述。
_Avoid_: 总结、复盘文档、review paper

### 账号与成本

**候选人档案**:
从一份简历中提取出的结构化经历描述（项目、职责、技术栈等），是生成**私有题集**与定制**模拟面试**的输入。它不保存简历原文。
_Avoid_: 简历、简历文件、resume

**目标岗位**:
候选人本次求职的岗位方向，是其个人属性、可变，不是平台的固定属性。它是**筛选维度**（"给我 ai 应用岗的题"），**不是知识组织维度** —— 知识地图里没有岗位这一层。
_Avoid_: 我的岗位、默认岗位、focus
> ⚠️ v1 把「岗位」同时写进知识体系与用户属性，两者耦合；v2 只保留后者的语义，并把岗位与**知识点**的关系做成独立的 `roles` / `role_points`（见 `docs/v2数据模型.md`）。

**额度点**:
用户每日可消耗的单一记账单位，按动作的真实成本比例扣减（一场**模拟面试**扣多点、一次**单题追问**扣一点、纯**题库**浏览不扣）。它是用户唯一需要理解的成本概念。
_Avoid_: 次数、配额、余额

**成本阀门**:
为控制 LLM 开销而设置的硬性上限（账号名额、并发数、每日调用量等）。它是运维参数，随预算直接调整，与产品和质量无关。
_Avoid_: 限流、配额

**阈值标定**:
用实验重新确定那些依赖语料与岗位分布的参数（去重相似度、配额比例等）。v1 的这些值只在「单岗位 + 0 真实用户」下成立，v2 不得沿用。
_Avoid_: 调参、常量、魔法数字

### 代码领域（模块边界）

**领域**:
代码一级目录的单位，每个领域自含其路由、服务与逻辑，**领域之间互不 import**。当一个动作同时需要两个领域时，放进能依赖两者的上一层，而不是让它们互相 import。理由见 `docs/adr/0005-code-is-split-by-domain.md`。
_Avoid_: 模块、包、feature、技术层

**web（接口层）**:
唯一**同时认识多个领域**的模块：它把各**领域**暴露的装配函数组合成整页，并持有页面级路由。**它不含业务规则** —— 一旦允许，`web` 就会开始持有判断逻辑，而它 import 一切，是唯一无法被隔离测试的模块。
**它按页面切文件**（一个文件对应一个 URL），不是一个可无限变大的 `pages.py` —— 这样"再加一个页面"是"再加一个文件"，而不是"往同一个文件里再写一段"。
_Avoid_: 前端、视图层、pages.py、控制器

**bank（题库领域）**:
管**题目本身** —— 题目怎么存与怎么查、它挂在哪个**知识点**下、**私有题集**、**晋升**、**内容门禁**。**写题目表的只有它。**
_Avoid_: questions 模块、题库服务

**knowledge（知识领域）**:
管**知识本身** —— **知识地图**、**知识点**、**考察点**、**命中状态**的聚合与**薄弱点**。它从不判定"哪道题挂错了"。
_Avoid_: 知识点模块、图谱模块

**offline（离线领域）**:
跑**离线管道**（聚类 / 挂载 / 生成 / 质检 / 自修复统计），也是**跨领域动作的固定去处** —— 挂载自修复住在这里，而不是住在 `knowledge`，因为**写题目表的必须是 `bank` 自己**。
_Avoid_: 任务队列、worker、后台任务
