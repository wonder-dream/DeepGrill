pyyaml
pydantic
python-dotenv
sqlmodel
openai
httpx
beautifulsoup4
pytest
apscheduler
fastapi
uvicorn
sentence-transformers
# torch 锁定 GPU 版（来自 pytorch cu124 index；lock 文件经 --extra-index-url 生成，
# 勿用 PyPI 默认 CPU 版覆盖）：
torch==2.6.0+cu124
# 文档解析（PDF/DOCX 走 MinerU，GPU torch 兼容；DOC 需系统 LibreOffice）
mineru[pipeline]
torchvision==0.21.0+cu124
accelerate
# 复习卷讲义 markdown 转 HTML（富格式渲染，为后续升级准备）
Markdown
# 讲义 HTML 白名单消毒（防存储型 XSS：LLM 输出/题库题干注入）
bleach


# ֪ʶ������������FAISS �ڴ������㣬�����Դ� SQLite��
faiss-cpu
