# AGENTS.md — DeepGrill v2 工作规则

## 动手前必须读

| 你要做的事 | 先读 |
|---|---|
| 改任何代码 | `CONTEXT.md`（术语的确切意思）+ `docs/v2范围基线.md`（In / Out 边界） |
| 改架构、分层、选型 | `docs/adr/` —— 已定决策与**被否决的备选方案**都在里面，别重新发明 |
| 迁移 v1 的任何行为 | `docs/v1行为规格.md` —— 哪些契约必须保留、哪些数字必须重新实验，逐条标好了 |
| 改数据模型 | `docs/v2数据模型.md` —— 表结构 |

**做出一项新的架构决策时：先写 ADR，再写代码。** 判定标准是三条同时成立 —— 难反悔、未来读者会奇怪"为什么这么做"、当时有真实的备选方案。

---

## 一、编码原则（继承 v1）

### 1. Think Before Coding
- 实现前显式陈述你的假设，不确定就停下来问
- 如果有多种理解方式，列出选项让我选，不要默默挑一个
- 发现更简单的方法时主动提出

### 2. Simplicity First
- 只解决被要求的问题，不加 speculative 功能
- 不复用一次的代码搞抽象
- 不加未被要求的配置 / 灵活性
- 自检：资深工程师会觉得这过度设计吗？如果是，重写

### 3. Surgical Changes
- 只修改与需求直接相关的代码
- 不改无关的相邻代码、注释、格式
- 匹配现有代码风格，即使你不喜欢
- 发现无关死代码可以提一句，但不要顺手删除
- 检验标准：每一行修改都能追溯到用户的具体请求

### 4. Goal-Driven Execution
- 优先定义"成功标准"，然后让模型循环验证
- 把"加验证"改成"写无效输入测试并让它们通过"
- 把"修 bug"改成"写复现测试并让它通过"

---

## 二、文档纪律

1. **加功能 / 砍功能，先改 `docs/v2范围基线.md`，再动代码。** 未列入 In 的一律不做 —— 这条是为了不重演 v1 的「5 周 8 次方向反转」。
2. **术语以 `CONTEXT.md` 为准。** 如果你发现自己需要一个 `CONTEXT.md` 里没有的词，或要改变已有词的意思 —— 停下来，先把术语定清楚。一个人在项目里最容易退化的不是代码，是词。
3. **`CONTEXT.md` 只放术语，不放实现细节、不放范围陈述、不放会变的数字。** 它越纯，读的人越不容易误判。
4. **不许把文档当事实源。** v1 的测试数在 65 份文档里有 13 个不同的取值、无一正确。需要事实就去读代码或跑一次。文档里只放**意图与理由**，不放**状态与数量**。

---

## 三、项目硬规则（每条都对应 v1 的一个真实事故）

### 3.1 降级可以，静默不行
v1 有 15 处 `except Exception` 捕获后只写日志、不 re-raise，导致「界面显示成功但数据没落库」和「会话永远停在判分中」。
**任何降级 / 吞异常都必须同时产出：① 用户可见的状态 ② 可查询的失败记录。**

### 3.2 任何进内存的东西都要有回收者
v1 的 `_upload_tasks` 每条永久泄漏（无任何 pop）、限流计数永不淘汰。
**新增任何进程内状态（缓存、任务表、计数器、锁）时，必须同时回答：谁回收它、TTL 多少、容量上限多少。**

### 3.3 禁止在 async 上下文里做同步 DB 查询
v1 在 async 中间件里同步查库，是它唯一阻塞事件循环的地方，SQLite 写锁争用时全部并发请求停止派发。

### 3.4 业务逻辑不许写在路由处理器里
v1 的 `routes.py` 长到 3033 行、`create_app` 单函数 1751 行，就是因为每轮迭代都往同一个函数里叠一层。**路由层只做参数校验、鉴权、调用与响应；编排与规则放 service 层。**

### 3.5 私有题集与公共题库同表 —— 查询必须过滤
`questions` 同时装公共题库与所有人的私有题集。**任何题目查询都必须经过统一的查询入口，不允许业务代码直接 `select(Question)`。** 漏一次 `owner_user_id` 过滤就是数据泄露。

### 3.6 不许全量加载大表
目标机器是 2C2G。v1 的「全表 embedding 拉成矩阵（每请求 O(N) 内存）」在这台机器上是定时炸弹。分页、流式，或落到数据库里算。

### 3.7 迁移与启动分离
v1 的 `_migrate_users_email` 会在启动时 `DELETE` 六张子表 + `DROP TABLE users`。**迁移必须是显式的、可审计的、与进程启动无关的步骤。**

### 3.8 每次调 LLM 都要能回答"这次花在哪"
模型与成本是 v2 的硬约束（额度点）。新增任何 LLM 调用时，必须明确它属于**实时**（用户直等）还是**离线**（可排队），并计入额度账本。

---

## 四、v2 的技术前提（不要推翻它们）

- **嵌入走 API，不跑本地模型。** 本地 bge-m3 需约 2.3GB 常驻内存，在 2C2G 上根本起不来。
- **面试官是一条 workflow（带循环的状态机），不是自主 Agent。** 控制流由代码持有、模型只在节点内自主；**否决权在代码手里**（`max_rounds` 到顶时强制收尾）—— 这就是 workflow 与 agent 的分界线。按静态 DAG 或固定步骤链来实现它会卡在「循环 + 挂起恢复」上，所以状态必须显式落库。理由见 ADR-0001。
- **知识层是关系库里的图**，不用图数据库，不做页面互链。理由见 ADR-0002。
- **面试报告由数据拼装**，只有一段总结由 LLM 生成。理由见 ADR-0003。
- **前端是零构建薄壳，不承载业务逻辑**，且 UI 形态限定为「单页状态机 + 对话框」。理由见 ADR-0004。
- **掌握度只看考察点命中记录**，与题目挂在哪个知识点下无关。
- **一次性重写**：不复制 v1 的实现代码，但 `docs/v1行为规格.md` 里标记为「继承」的契约必须遵守。

---

## 五、v1 代码库的位置

只读参考：`D:\document\InterviewAssistant`（`main` 分支，baseline commit `98daeb8`）。

- 它是**规格来源与数据来源**，不是代码来源
- 3009 道题与它们的 `good_criteria` / `bad_criteria` 要从它的 `data/interview.db` 导出（SQLite 只读连接）
- **不要修改它** —— 迁移与知识层构建都依赖它保持原样
